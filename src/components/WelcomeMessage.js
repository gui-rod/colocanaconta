import React from 'react';
import {StyleSheet, Text} from "react-native";

export default () => <Text style={styles.f20}>Open up App.js to start working on your app! :)</Text>

const styles = StyleSheet.create({
    f20: {
        fontSize: 20
    }
});